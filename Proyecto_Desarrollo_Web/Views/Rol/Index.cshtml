@using Proyecto_Desarrollo_Web.Models.ViewModel;
@model List<RolVm>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<head>
    <link rel="stylesheet" type="text/css" href="~/css/Tablas.css" />
</head>
<h1>Rol</h1>

<form asp-action="Insertar" method="get">
    <div class="row">
        <div class="danger">
            @if (TempData["mensaje"] != null)
            {
                <div class="alert alert-warning alert-dismissible" role="alert">
                    <p>@TempData["mensaje"]</p>
                    <button type="button" class="close" data-dismiss="alert" aria-label="close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            }

        </div>
    </div>

    <div class="form-group">
        <input type="submit" value="Agregar" class="btn-success">
    </div>
    @if (Model.Count > 0)
    {
        <div class="ScrollB">
            <table class="table table-bordered table-striped my-table">
                <tread>
                    <tr>
                        <th>Descripcion</th>
                        <th>Descripcion 2</th>
                        <th>Acciones</th>
                    </tr>
                </tread>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Descripcion</td>
                            <td>@item.Descripcion2</td>
                            <td>
                                @Html.ActionLink("Editar", "Editar", "Rol", new {@Id = item.Id}, new{@class = "btn btn-outline-primary"})
                                @Html.ActionLink("Eliminar", "Eliminar", "Rol", new {@Id = item.Id}, new{@class = "btn btn-outline-danger"})
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</form>

